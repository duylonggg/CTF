

# This file was *autogenerated* from the file solve.sage
from sage.all_cmdline import *   # import sage library

_sage_const_0xbde3c425157a83cbe69cee172d27e2ef9c1bd754ff052d4e7e6a26074efcea673eab9438dc45e0786c4ea54a89f9079ddb21 = Integer(0xbde3c425157a83cbe69cee172d27e2ef9c1bd754ff052d4e7e6a26074efcea673eab9438dc45e0786c4ea54a89f9079ddb21); _sage_const_0x686be42f9c3f431296a928c288145a847364bb259c9f5738270d48a7fba035377cc23b27f69d6ae0fad76d745fab25d504d5 = Integer(0x686be42f9c3f431296a928c288145a847364bb259c9f5738270d48a7fba035377cc23b27f69d6ae0fad76d745fab25d504d5); _sage_const_5 = Integer(5); _sage_const_7 = Integer(7); _sage_const_2 = Integer(2); _sage_const_12 = Integer(12); _sage_const_4 = Integer(4); _sage_const_3 = Integer(3); _sage_const_0 = Integer(0); _sage_const_1 = Integer(1); _sage_const_8 = Integer(8); _sage_const_32 = Integer(32); _sage_const_126 = Integer(126)# solve.sage

# ---- Params from challenge ----
p  = _sage_const_0xbde3c425157a83cbe69cee172d27e2ef9c1bd754ff052d4e7e6a26074efcea673eab9438dc45e0786c4ea54a89f9079ddb21 
Xt = _sage_const_0x686be42f9c3f431296a928c288145a847364bb259c9f5738270d48a7fba035377cc23b27f69d6ae0fad76d745fab25d504d5 

F  = GF(p)
E  = EllipticCurve(F, [_sage_const_5 , _sage_const_7 ])

# Lấy r = prime factor thứ 4 (index 3) của |E| với small factors
fac = E.order().factor(limit=_sage_const_2 **_sage_const_12 )
if len(fac) < _sage_const_4 :
    raise RuntimeError(f"Không đủ small factors, hiện có: {fac}")
r = ZZ(fac[_sage_const_3 ][_sage_const_0 ])
print(f"[i] |E| factors (small): {fac}")
print(f"[i] r =", r)

def to_ascii_from_int(xi: Integer):
    # big-endian, bỏ leading zero-length
    nbytes = max(_sage_const_1 , (xi.nbits()+_sage_const_7 )//_sage_const_8 )
    bs = xi.to_bytes(nbytes, 'big', signed=False)
    # chỉ nhận ASCII in được để giống flag content
    if any(b < _sage_const_32  or b > _sage_const_126  for b in bs):
        return None
    try:
        return bs.decode('ascii')
    except:
        return None

def check_inner(inner: str) -> bool:
    # Verify theo đúng chall: x = int.from_bytes(inner.encode(), 'big')
    x_int = Integer(int.from_bytes(inner.encode('ascii'), 'big'))
    try:
        P = E.lift_x(x_int)     # Sage tự mở rộng trường nếu cần
    except Exception:
        return False
    Q = r * P
    return Integer(Q[_sage_const_0 ]) % p == Xt % p

# Dựng Q với x = Xt (trên F hoặc F_{p^2})
cands = []
rhs = F(Xt)**_sage_const_3  + F(_sage_const_5 )*F(Xt) + F(_sage_const_7 )
if rhs.is_square():
    y = rhs.sqrt()
    cands = [E(F(Xt),  y), E(F(Xt), -y)]
else:
    K = GF(p**_sage_const_2 , names=('u',)); (u,) = K._first_ngens(1)
    EK = EllipticCurve(K, [K(_sage_const_5 ), K(_sage_const_7 )])
    xK = K(Xt)
    rhsK = xK**_sage_const_3  + K(_sage_const_5 )*xK + K(_sage_const_7 )
    if not rhsK.is_square():
        raise RuntimeError("Không dựng được Q ở cả GF(p^2) (bất thường).")
    yK = rhsK.sqrt()
    cands = [EK(xK,  yK), EK(xK, -yK)]

solutions = set()

for Q in cands:
    K = Q.curve().base_ring()
    EK = E.change_ring(K)
    N  = EK.cardinality()
    # tách N = r^e * M (gcd(M,r)=1)
    e  = _sage_const_0 
    T  = N
    while T % r == _sage_const_0 :
        T //= r
        e += _sage_const_1 
    M  = T
    print(f"[i] Field: GF(p^{K.degree()}), #E(K) = r^{e} * {M}")

    # --- (1) preimage nhanh: R = u * Q với u ≡ r^{-1} (mod M)
    try:
        u = inverse_mod(r, M)
        R = Integer(u) * Q
        xR = R[_sage_const_0 ]
        if xR in F:
            s = to_ascii_from_int(Integer(xR))
            if s:
                if check_inner(s):
                    solutions.add(f"ictf{{{s}}}")
                    print("[+] Found via inverse-trick:", f"ictf{{{s}}}")
    except ZeroDivisionError:
        pass

    # --- (2) Thử liệt kê r-division nếu chưa ra
    if not solutions:
        print("[i] Enumerating r-division points ...")
        divs = Q.division_points(r)
        print(f"[i] Got {len(divs)} preimages")
        for P in divs:
            xP = P[_sage_const_0 ]
            if xP in F:
                s = to_ascii_from_int(Integer(xP))
                if s and check_inner(s):
                    solutions.add(f"ictf{{{s}}}")
                    print("[+] Found via division_points:", f"ictf{{{s}}}")
                    break

if solutions:
    print("\n=== FLAG ===")
    for f in solutions:
        print(f)
else:
    print("[-] Chưa tìm được. Gợi ý: tăng factor(limit), hoặc thử cả GF(p^k) với k=4 (hiếm).")

